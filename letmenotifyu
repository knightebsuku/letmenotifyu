#!/usr/bin/python3

#version 1.7

import os
import sys

from notifylib.create import create_database
from notifylib.base import Base

#sys.path.append("/usr/share/letmenotifyu/")

os.chdir("ui")
directory_path=os.environ['HOME']+'/.local/share/letmenotifyu/'
db_file=directory_path+'letmenotifyu7.sqlite'

if __name__ == "__main__":
    if os.path.isfile(db_file):
        Base('main.glade', 'letmenotifyu.png', db_file)
    else:
        os.makedirs(directory_path)
        create_database(db_file)
        Base('main.glade', 'letmenotifyu.png', db_file)
   
